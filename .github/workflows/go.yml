name: golang pkg starter

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  go-ctrd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.15
      - run: mkdir -p /home/runner/go/src/github.com/ModEtchFill
      - run: ln -s /home/runner/work/base-i/base-i /home/runner/go/src/github.com/ModEtchFill
      - name: Build
        run: go build -v ./...
      - name: Test a
        run: go test -v github.com/ModEtchFill/base-i/aTest
      - name: Test b
        run: go test -v github.com/ModEtchFill/base-i/bTest
